name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install client dependencies
        run: npm ci

      - name: Install server dependencies
        run: npm ci
        working-directory: server

      - name: Audit dependencies (client)
        run: npm audit --audit-level=critical

      - name: Audit dependencies (server)
        run: npm audit --audit-level=critical
        working-directory: server

      - name: Lint (client)
        run: npx biome check src/

      - name: Lint (server)
        run: npx biome check src/
        working-directory: server

      - name: Type check (client)
        run: npx tsc --noEmit

      - name: i18n lint (missing/unused translation keys)
        run: npx i18next-cli lint

      - name: Type check (server)
        run: npx tsc --noEmit
        working-directory: server

      - name: Test (client)
        run: npx vitest run

      - name: Test (server)
        run: npx vitest run
        working-directory: server

      - name: Build client
        run: npx vite build

      - name: Build server
        run: npm run build
        working-directory: server
